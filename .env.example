# Copy to .env and fill in. Never commit .env.
#
# Used only by sift/classify/ for the single batched classification call.
# The scanner and catalog layers never touch this.

OPENAI_API_KEY=sk-your-key-here
SIFT_MODEL=gpt-4o-mini
